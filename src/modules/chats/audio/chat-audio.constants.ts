/** Texto placeholder da UI/memória do bot para mensagem só com áudio (antes da transcrição). */
export const CHAT_AUDIO_PLACEHOLDER = '[Áudio]';
/** Prefixo fixo para os objetos de áudio do WhatsApp no bucket R2. */
export const R2_WHATSAPP_AUDIO_PREFIX = 'whatsapp/audio/';
/** TTL (segundos) da URL presigned devolvida pela listagem de mensagens. */
export const AUDIO_SIGNED_URL_TTL_SECONDS = 60 * 60;
